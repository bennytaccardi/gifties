model Merchant {
  id    String     @id
  storeDescription String?
  storeName String?
  storeUrl String?
}

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["driverAdapters"]
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}